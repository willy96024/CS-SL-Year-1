print("""September 8th notes


-MENU-
---------
Please choose one of the options below, and press enter

1) Debugging
2) Validation vs verification
3) Testing & types of testing
4) Dry-run
5) Functional
6) Unit
7) Integration
8) User acceptance
9) Alpha
10) Beta
11) Types of data used in testing
12) normal
13) at the limits
14) extreme
15) abnormal


""")

answer = "empty"
userchoice = input()

while(answer == "empty"):
  answer = "chosen"
  if(userchoice == "1"):
    print("process of correcting (and finding) bugs or errors in a program is referred to as debugging.
")
  elif(userchoice == "2"):
    print("Verification and validation are independent procedures that are used together for checking that a product, service, or system meets requirements and specifications and that it fulfills its intended purpose
")
  elif(userchoice == "3"):
    print("Testing is used to check for potential costly bugs, verify the system and related processes run successfully, as well as to validate that the product or system meets the requirements. Two types of testing, dynamic and static
")
  elif(userchoice == "4"):
    print("dry-run is when you manually move through the code with pen-and-paper, to determine the expected outcome when the code is executed
")
  elif(userchoice == "5"):
    print("testing individual commands, text input, menu functions, etc.
")
  elif(userchoice == "6"):
    print("relates to separately testing the individual components or parts of the system or software.
")
  elif(userchoice == "7"):
    print("where the components are testing together within the system or software to verify that everything works correctly with each other
")
  elif(userchoice == "8"):
    print("relates to determining if the system satisfies the customer's needs
")
  elif(userchoice == "9"):
    print("In alpha testing, the software or system is released to those within the company or organization who are not specifically involved in the development or implementation of the product
")
  elif(userchoice == "10"):
    print("In beta testing, the software system is available to the public (or a group consisting of the public)")
    
  elif(userchoice == "11"):
    print("normal data, extreme data, boundary data, abonrmal/erroneous data
")
  elif(userchoice == "12"):
    print("data that shoukd be accepted by the system(typical)
")
  elif(userchoice == "13"):
  print("a pair of values at each end of a range(data at the upper or lower limits of expectations that should be accepted, the immediate values before or beyond the limits of expectations that should be rejected)
")
  elif(userchoice == "14"):
  print("data at the upper or lower limjits of expectations that should be accepted by the system
")
  elif(userchoice == "15"):
  print("data that falls outside of what is acceptable and should be rejected by the system
")
else:
print("Invalid Code")
answer = "empty"
